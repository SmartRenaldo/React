import ReactDOM from 'react-dom/client';

const App = (
  <div>
    <h1>This is a React 18 project</h1>
    <p>Hello React 18!</p>
  </div>
);

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(App);
